name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: g++ -Wall -Wextra stats-test.cpp stats.cpp alert.cpp -o testchecker 
      
    - name: run
      run: ./testchecker
